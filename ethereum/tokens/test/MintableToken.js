const BigNumber = require('bignumber.js');
const MintableToken = artifacts.require("MintableToken");

contract('MintableToken', function(accounts)
{
  it("Test successful minting of tokens.", async () =>
  {
    // Checks successful minting of tokens.
    // Checks balance before minting.
    // Checks balance after minting.
  });
})
